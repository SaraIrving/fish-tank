class AnglerFish extends Fish {
  constructor(options) {
    super(options); // Call super to run the code inside `Fish`'s constructor
    this.imageUri = '/images/angler-fish.png'; // Set the image
  }

}
